{
  "entities": {
    "GlobalGameStatus": {
      "title": "Global Game Status",
      "description": "Shared compatibility and reliability data for a specific game across all users.",
      "type": "object",
      "properties": {
        "gameId": { "type": "string", "description": "Unique ID of the game." },
        "status": { "type": "string", "enum": ["compatible", "unstable", "broken", "untested"], "description": "Current verified status." },
        "reliabilityScore": { "type": "number", "description": "Score from 0 to 1 based on user reports." },
        "lastVerifiedAt": { "type": "string", "format": "date-time", "description": "Last time the status was updated." },
        "verifiedBy": { "type": "string", "description": "UID of the admin or system that verified it." },
        "romUrl": { "type": "string", "description": "The current working ROM URL." },
        "emulatorCore": { "type": "string", "description": "The recommended emulator core." },
        "successCount": { "type": "integer", "description": "Number of successful loads reported by users." },
        "failureCount": { "type": "integer", "description": "Number of failed loads reported by users." }
      },
      "required": ["gameId", "status", "lastVerifiedAt"]
    },
    "TelemetryReport": {
      "title": "Telemetry Report",
      "description": "Aggregated error reports and events sent by users.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "UID of the reporting user." },
        "timestamp": { "type": "string", "format": "date-time" },
        "type": { "type": "string", "enum": ["error", "performance", "navigation", "rom_fetch", "emulator"], "description": "Type of the report." },
        "details": { "type": "object", "description": "Flexible JSON object with error details." },
        "appVersion": { "type": "string" }
      },
      "required": ["timestamp", "type"]
    },
    "AdminConfig": {
      "title": "Admin Configuration",
      "description": "Global settings for the RetroVerse OS ecosystem.",
      "type": "object",
      "properties": {
        "masterArchiveId": { "type": "string", "description": "The primary Archive.org collection ID." },
        "maintenanceMode": { "type": "boolean" },
        "minAppVersion": { "type": "string", "description": "Minimum version required to use the app." }
      }
    }
  },
  "firestore": {
    "/global_catalog/{gameId}": {
      "schema": "GlobalGameStatus",
      "description": "The master registry of game compatibility."
    },
    "/telemetry/{reportId}": {
      "schema": "TelemetryReport",
      "description": "User-submitted telemetry data."
    },
    "/config/global": {
      "schema": "AdminConfig",
      "description": "Global application configuration."
    }
  }
}
